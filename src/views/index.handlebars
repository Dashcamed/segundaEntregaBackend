<h1>Products with Websockets</h1>

<div class="container" id="productList">
  {{#each products}}
    <div class="card-product">
      <h3>{{title}}</h3>
      <p>{{description}}</p>
      <p>${{price}}</p>
      <p>{{stock}}</p>
    </div>
  {{/each}}
</>